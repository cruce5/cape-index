node_modules/
.DS_Store
*.log
.wrangler/
dist/
build/

# scrape caches (BOM HTML, OMDb JSON, Wikipedia wikitext) are all regenerable
# from the scripts + additions.json — kept off the repo to keep it lean
data/raw/*
!data/raw/.gitkeep

# local secrets (OMDb key etc.) — keep the .example, ignore the real one
scripts/local.env.ps1
.env
.env.local
.reconcilers/
