node_modules/
.DS_Store
*.log
.wrangler/
dist/
build/

# raw scrape caches (HTML title pages + the wikicast dumps) are regenerable
data/raw/*.html
data/raw/wikicast-*.txt
!data/raw/.gitkeep

# local secrets (OMDb key etc.) — keep the .example, ignore the real one
scripts/local.env.ps1
.env
.env.local
